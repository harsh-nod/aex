{
  "name": "@aex-lang/langgraph",
  "version": "0.0.1",
  "description": "LangGraph compiler for AEX contracts",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "vitest run --dir test"
  },
  "dependencies": {
    "@aex-lang/parser": "file:../aex-parser",
    "@aex-lang/validator": "file:../aex-validator"
  }
}
