# LangGraph

Compile an AEX contract into a guarded LangGraph flow. Each `do`, `make`, `check`, and `confirm` becomes an explicit node with policy enforcement.

Implementation is scheduled after the CLI and local runtime stabilize.
