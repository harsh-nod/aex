{
  "name": "@aex/monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "website"
  ],
  "scripts": {
    "build": "npm run build -w @aex/parser && npm run build -w @aex/validator && npm run build -w @aex/runtime && npm run build -w @aex/cli && npm run build -w @aex/openai-agents && npm run build -w @aex/mcp-gateway",
    "dev": "npm run dev -w @aex/cli",
    "lint": "npm run lint -w @aex/cli",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cli": "npm run test -w @aex/cli",
    "docs:dev": "npm run dev -w @aex/website",
    "docs:build": "npm run build -w @aex/website"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "eslint": "^9.2.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.29.1",
    "prettier": "^3.2.5",
    "typescript": "^5.4.5",
    "vitest": "^4.1.5"
  }
}
